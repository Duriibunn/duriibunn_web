# 여행 스타일 기반 맞춤 추천 기능 🎯

## 개요
사용자가 선택한 여행 스타일에 맞춰 관광지, 음식점, 문화시설, 숙박 등의 장소를 지능적으로 필터링하고 우선순위를 조정하여 보여주는 기능입니다.

## 주요 기능

### 1. 🔍 키워드 기반 스마트 매칭
각 여행 스타일에 맞는 키워드를 기반으로 장소를 자동 분석하고 점수를 계산합니다.

#### 여행 스타일별 키워드
- **🎢 체험 액티비티**: 체험, 액티비티, 레저, 스포츠, 모험, 놀이, 공원, 테마파크
- **📸 SNS 핫플레이스**: 인스타, 포토존, 카페, 핫플, 전망, 뷰, 맛집, 유명
- **🏞️ 자연과 함께**: 자연, 산, 바다, 숲, 계곡, 폭포, 해변, 트레킹, 등산
- **🗺️ 유명 관광지는 필수**: 관광, 명소, 유명, 랜드마크, 박물관, 전시, 기념관
- **🧘 여유롭게 힐링**: 힐링, 휴식, 온천, 스파, 조용, 평화, 여유, 산책
- **🎨 문화 예술 역사**: 문화, 예술, 역사, 전통, 한옥, 사찰, 유적, 갤러리, 공연
- **🏘️ 여행지 느낌 물씬**: 로컬, 골목, 마을, 시장, 전통, 향토, 민속
- **🛍️ 쇼핑은 열정적으로**: 쇼핑, 백화점, 아울렛, 면세점, 거리, 상가, 몰
- **🍜 관광보다 먹방**: 맛집, 음식, 먹거리, 식당, 레스토랑, 카페, 디저트, 특산물

### 2. 📊 점수 기반 정렬 시스템
장소의 이름, 설명, 주소에서 키워드를 검색하여 매칭 점수를 계산합니다.
- 매칭되는 키워드 1개당 점수 +1
- 점수가 높을수록 상위에 표시
- 점수가 0보다 큰 장소에는 "✨ 추천" 배지 표시

### 3. 🎯 카테고리별 추천
여행 스타일에 따라 추천 카테고리를 자동으로 표시합니다.

#### 스타일별 추천 카테고리
- **체험 액티비티** → 관광지 ✨, 문화시설 ✨
- **SNS 핫플레이스** → 음식점 ✨, 관광지 ✨
- **자연과 함께** → 관광지 ✨
- **유명 관광지는 필수** → 관광지 ✨, 문화시설 ✨
- **여유롭게 힐링** → 숙박 ✨, 관광지 ✨
- **문화 예술 역사** → 문화시설 ✨, 관광지 ✨
- **여행지 느낌 물씬** → 음식점 ✨, 관광지 ✨
- **쇼핑은 열정적으로** → 관광지 ✨
- **관광보다 먹방** → 음식점 ✨

### 4. 🔀 필터 토글 기능
사용자가 표시 방식을 선택할 수 있습니다:
- **맞춤 장소만 보기**: 여행 스타일에 매칭되는 장소만 표시 (점수 > 0)
- **전체 장소 보기**: 모든 장소를 표시하되, 매칭 점수 순으로 정렬

## UI 구성

### 1. 여행 스타일 표시 패널
```
✨ 선택한 여행 스타일에 맞는 장소 추천
🏞️ 자연과 함께  🧘 여유롭게 힐링  🍜 관광보다 먹방
[맞춤 장소만 보기] 버튼
```

### 2. 카테고리 탭
추천 카테고리에는 ✨ 아이콘이 표시됩니다:
```
✨ 🏛️ 관광지 [100]    🍴 음식점 [100]    🎭 문화시설 [50]    🏨 숙박 [80]
   ^추천                  ^추천
```

### 3. 장소 카드
매칭되는 장소에는 "✨ 추천" 배지가 표시됩니다:
```
┌─────────────────┐
│ [✨ 추천]      │ ← 추천 배지
│                 │
│   한라산 백록담  │
│   제주특별자치시 │
└─────────────────┘
```

## 알고리즘 상세

### calculateStyleScore 함수
```typescript
function calculateStyleScore(place: Place, travelStyles: string[]): number {
  let score = 0;
  const searchText = `${place.name} ${place.description || ''} ${place.address || ''}`.toLowerCase();
  
  travelStyles.forEach(style => {
    const keywords = STYLE_KEYWORDS[style] || [];
    keywords.forEach(keyword => {
      if (searchText.includes(keyword.toLowerCase())) {
        score += 1;
      }
    });
  });
  
  return score;
}
```

### 정렬 로직
```typescript
const currentPlaces = (() => {
  const places = placesByCategory[selectedCategory] || [];
  const travelStyles = tripData?.travelStyle || [];
  
  // 여행 스타일이 없으면 원본 반환
  if (travelStyles.length === 0) return places;
  
  // 점수 계산
  const placesWithScore = places.map(place => ({
    place,
    score: calculateStyleScore(place, travelStyles)
  }));
  
  // 필터링 (맞춤 장소만 or 전체)
  const filtered = showStyleMatched 
    ? placesWithScore.filter(item => item.score > 0)
    : placesWithScore;
  
  // 점수 높은 순으로 정렬
  return filtered
    .sort((a, b) => b.score - a.score)
    .map(item => item.place);
})();
```

## 사용 예시

### 예시 1: 자연 힐링 여행
**선택한 스타일**: 🏞️ 자연과 함께, 🧘 여유롭게 힐링

**추천 결과** (관광지 카테고리):
1. ✨ 한라산 백록담 (점수: 5) - "자연", "산", "등산"
2. ✨ 성산일출봉 (점수: 4) - "자연", "산", "전망"
3. ✨ 협재해수욕장 (점수: 3) - "바다", "해변", "휴식"
4. ✨ 비자림 (점수: 3) - "숲", "산책", "평화"
5. 제주민속촌 (점수: 0) - 매칭 없음

### 예시 2: 먹방 여행
**선택한 스타일**: 🍜 관광보다 먹방, 🏘️ 여행지 느낌 물씬

**추천 결과** (음식점 카테고리):
1. ✨ 동문재래시장 먹거리 거리 (점수: 7) - "시장", "먹거리", "전통", "로컬"
2. ✨ 제주 흑돼지 맛집 (점수: 4) - "맛집", "특산물"
3. ✨ 올레시장 골목 식당 (점수: 5) - "시장", "골목", "식당", "로컬"
4. 스타벅스 제주점 (점수: 0) - 매칭 없음

### 예시 3: 문화 역사 여행
**선택한 스타일**: 🎨 문화 예술 역사

**추천 결과** (문화시설 카테고리):
1. ✨ 국립제주박물관 (점수: 3) - "문화", "역사", "박물관"
2. ✨ 제주돌문화공원 (점수: 2) - "문화", "전통"
3. ✨ 관덕정 (점수: 4) - "역사", "전통", "유적", "문화"
4. 제주아쿠아플라넷 (점수: 0) - 매칭 없음

## 데이터 흐름

```
1. CreateTripPage
   ↓ (사용자가 여행 스타일 선택)
   companion: 'couple'
   travelStyle: ['nature', 'healing', 'food']
   ↓ (sessionStorage 저장)

2. SelectPlacesPage
   ↓ (sessionStorage에서 불러오기)
   tripData.travelStyle = ['nature', 'healing', 'food']
   ↓ (장소 데이터 로드)
   placesByCategory = { attraction: [...], restaurant: [...], ... }
   ↓ (점수 계산 및 정렬)
   calculateStyleScore(각 장소, travelStyle)
   ↓ (UI 표시)
   - 여행 스타일 표시
   - 추천 카테고리에 ✨ 표시
   - 매칭 장소에 "✨ 추천" 배지
   - 점수 순으로 정렬
```

## 기술 구현

### 데이터 구조
```typescript
// 여행 스타일 키워드 매핑
const STYLE_KEYWORDS: Record<string, string[]> = {
  activity: ['체험', '액티비티', ...],
  nature: ['자연', '산', ...],
  // ...
};

// 스타일별 추천 카테고리
const STYLE_TO_CATEGORY: Record<string, string[]> = {
  activity: ['attraction', 'culture'],
  food: ['restaurant'],
  // ...
};
```

### 상태 관리
```typescript
const [tripData, setTripData] = useState<{
  city: string;
  cityName: string;
  startDate: string;
  endDate: string;
  days: number;
  title: string;
  companion?: string;
  travelStyle?: string[]; // 여행 스타일 배열
} | null>(null);

const [showStyleMatched, setShowStyleMatched] = useState(true); // 필터 토글
```

## 향후 개선 방향

### 1. 머신러닝 기반 추천
- 사용자 선택 패턴 학습
- 유사 사용자 기반 협업 필터링
- 장소 인기도 데이터 활용

### 2. 더 정교한 매칭 알고리즘
- TF-IDF 알고리즘 적용
- 키워드 가중치 부여 (중요도에 따라)
- 시맨틱 검색 (의미 기반 매칭)

### 3. 실시간 데이터 활용
- 실시간 리뷰 데이터
- 계절/날씨에 따른 추천 조정
- 혼잡도 정보 반영

### 4. 개인화 강화
- 이전 여행 기록 분석
- 사용자 선호도 프로필 구축
- 동반자에 따른 추천 조정

### 5. 소셜 기능
- 다른 사용자의 여행 스타일 참고
- 추천 장소 공유
- 리뷰 및 평점 시스템

## 성능 최적화

### 현재 구현
- 모든 카테고리 데이터를 한 번에 병렬 로드 (Promise.all)
- 카테고리당 100개 장소 (총 ~400개)
- 점수 계산은 클라이언트에서 즉시 수행
- 탭 전환 시 재계산 (데이터는 이미 로드됨)

### 최적화 가능한 부분
1. **점수 캐싱**: 한 번 계산한 점수를 메모이제이션
2. **가상 스크롤**: 100개 이상의 장소를 효율적으로 렌더링
3. **Web Worker**: 점수 계산을 백그라운드 스레드에서 수행
4. **서버사이드 필터링**: API에서 직접 필터링된 데이터 제공

## 결론
이 기능은 사용자의 여행 스타일을 실제 장소 추천에 활용하여 개인화된 여행 계획을 수립할 수 있게 합니다. 키워드 기반 매칭을 통해 즉각적인 결과를 제공하며, 향후 머신러닝과 실시간 데이터를 활용하여 더욱 정교한 추천 시스템으로 발전할 수 있습니다.
